---
description: Frontend ve React/Next.js bileşen kuralları
globs: app/**/*.tsx, components/**/*.tsx
alwaysApply: false
---

# Frontend Kuralları

## 🔧 Next.js 14+ App Router

### Sayfa Yapısı
```typescript
// Her sayfa için metadata export et
export const metadata: Metadata = {
  title: 'Sayfa Başlığı | PromptMaster AI',
  description: 'SEO açıklaması...',
};

// Server Component varsayılan, client sadece gerektiğinde
export default function Page() {
  return <main>...</main>;
}
```

### Client vs Server Components
- `"use client"` sadece interaktif bileşenlerde kullan
- Data fetching server component'te yap
- useState, useEffect gereken yerde client component

---

## 🎨 Tailwind CSS Standartları

### Responsive Tasarım (Mobile First)
```tsx
<div className="
  w-full          // Mobile default
  md:w-1/2        // Tablet
  lg:w-1/3        // Desktop
">
```

### Dark Mode Renkleri
```tsx
// Arka planlar
bg-slate-900      // Ana arka plan (#0f172a)
bg-slate-800      // Kart arka planı
bg-slate-700      // Hover state

// Metinler
text-white        // Ana metin
text-slate-300    // İkincil metin
text-slate-500    // Disabled metin

// Aksanlar
text-violet-500   // Primary accent
bg-violet-600     // Button primary
hover:bg-violet-500
```

---

## 🧩 Bileşen Yapısı

### Standart Bileşen Template
```typescript
// components/UI/Button.tsx
import { type ReactNode } from 'react';

interface ButtonProps {
  children: ReactNode;
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  onClick?: () => void;
  disabled?: boolean;
}

export function Button({ 
  children, 
  variant = 'primary',
  size = 'md',
  onClick,
  disabled = false
}: ButtonProps) {
  const baseStyles = 'rounded-lg font-medium transition-all duration-200';
  
  const variants = {
    primary: 'bg-violet-600 hover:bg-violet-500 text-white',
    secondary: 'bg-slate-700 hover:bg-slate-600 text-white',
    ghost: 'bg-transparent hover:bg-slate-800 text-slate-300',
  };
  
  const sizes = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-4 py-2 text-base',
    lg: 'px-6 py-3 text-lg',
  };

  return (
    <button
      onClick={onClick}
      disabled={disabled}
      className={`${baseStyles} ${variants[variant]} ${sizes[size]} 
        disabled:opacity-50 disabled:cursor-not-allowed`}
    >
      {children}
    </button>
  );
}
```

---

## 🎬 Animasyonlar (Framer Motion)

```typescript
import { motion } from 'framer-motion';

// Fade in animasyonu
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.3 }}
>

// Hover efekti
<motion.button
  whileHover={{ scale: 1.02 }}
  whileTap={{ scale: 0.98 }}
>
```

---

## 📱 Mobile First Drawer Pattern

```typescript
// Mobilde drawer, desktop'ta sidebar
<div className="
  fixed inset-x-0 bottom-0 
  md:relative md:inset-auto
  bg-slate-800 rounded-t-2xl md:rounded-lg
  p-4 md:p-6
">
```
