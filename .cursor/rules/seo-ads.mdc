---
description: SEO ve AdSense entegrasyonu kuralları
globs: app/**/*.tsx, components/Ads/**/*
alwaysApply: false
---

# SEO & AdSense Kuralları

## 🔍 SEO Standartları

### Metadata Template
```typescript
// app/page.tsx
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Free AI Prompt Generator for Midjourney, DALL-E & Stable Diffusion',
  description: 'Create stunning AI art prompts visually. No memorization needed - just click and generate perfect prompts for Midjourney, DALL-E 3, and Stable Diffusion.',
  keywords: ['ai prompt generator', 'midjourney prompts', 'stable diffusion', 'dall-e'],
  openGraph: {
    title: 'PromptMaster AI - Visual Prompt Builder',
    description: 'Create AI art prompts visually',
    type: 'website',
    locale: 'en_US',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'PromptMaster AI',
    description: 'Visual AI Prompt Generator',
  },
};
```

### Schema Markup

#### Ana Sayfa (SoftwareApplication)
```typescript
const softwareSchema = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "PromptMaster AI",
  "applicationCategory": "DesignApplication",
  "operatingSystem": "Web",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "USD"
  },
  "description": "Visual AI prompt generator for Midjourney, DALL-E, and Stable Diffusion"
};
```

#### Blog Yazıları (Article)
```typescript
const articleSchema = {
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": post.title,
  "datePublished": post.date,
  "author": {
    "@type": "Organization",
    "name": "PromptMaster AI"
  }
};
```

---

## 📢 AdSense Yerleşim Planı

### 1. Header Banner (İnce Şerit)
```tsx
// components/Ads/HeaderAd.tsx
export function HeaderAd() {
  return (
    <div className="w-full bg-slate-800 py-2 text-center">
      <ins
        className="adsbygoogle"
        style={{ display: 'block' }}
        data-ad-client="ca-pub-XXXXXXX"
        data-ad-slot="XXXXXXX"
        data-ad-format="horizontal"
      />
    </div>
  );
}
```

### 2. Sonuç Kutusu Altı (EN DEĞERLİ!)
```tsx
// Kullanıcı "Kopyala" dedikten sonraki alan
<div className="mt-4 flex justify-center">
  <ins
    className="adsbygoogle"
    style={{ display: 'block', width: '300px', height: '250px' }}
    data-ad-client="ca-pub-XXXXXXX"
    data-ad-slot="XXXXXXX"
  />
</div>
```

### 3. Sidebar (Desktop Blog)
```tsx
// components/Ads/SidebarAd.tsx
export function SidebarAd() {
  return (
    <aside className="hidden lg:block sticky top-4 w-[300px]">
      <ins
        className="adsbygoogle"
        style={{ display: 'block' }}
        data-ad-client="ca-pub-XXXXXXX"
        data-ad-slot="XXXXXXX"
        data-ad-format="vertical"
      />
    </aside>
  );
}
```

### 4. In-Article (Her 3 Paragrafta Bir)
```tsx
// Blog içeriklerinde doğal yerleşim
{paragraphs.map((p, i) => (
  <>
    <p>{p}</p>
    {(i + 1) % 3 === 0 && <InArticleAd key={`ad-${i}`} />}
  </>
))}
```

---

## ⚖️ Yasal Sayfalar (AdSense Onayı İçin Zorunlu)

Footer'da şu linkler **MUTLAKA** olmalı:
- `/about` - About Us
- `/privacy` - Privacy Policy
- `/terms` - Terms of Service
- `/contact` - Contact Us
- `/cookies` - Cookie Policy

### GDPR Cookie Banner
```tsx
// Avrupa trafiği için zorunlu
export function CookieBanner() {
  const [accepted, setAccepted] = useState(false);
  
  if (accepted) return null;
  
  return (
    <div className="fixed bottom-0 left-0 right-0 bg-slate-800 p-4 z-50">
      <p className="text-slate-300 text-sm">
        We use cookies to improve your experience. 
        <a href="/cookies" className="text-violet-400 ml-1">Learn more</a>
      </p>
      <button 
        onClick={() => setAccepted(true)}
        className="mt-2 bg-violet-600 px-4 py-2 rounded text-white"
      >
        Accept
      </button>
    </div>
  );
}
```
